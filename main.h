#ifdef MAIN_H

/*
// 3/ works test comment 3
/**
 * test comment 2 */
class ScreenSize {		// may change between invokations of paginator.
/* test comment 1 */
public:
    int chars {80};		// number of printable fixed size characters that fit on one horizontal line.
    int lines {24};		// number of printable fixed size text lines that fit on the vertical dimension of a computer screen.
};
*/

#endif // MAIN_H
